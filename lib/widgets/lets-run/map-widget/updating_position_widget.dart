import 'package:flutter/material.dart';

class UpdatingPosition extends StatelessWidget {
  const UpdatingPosition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("FINDING POSITION...");
  }
}